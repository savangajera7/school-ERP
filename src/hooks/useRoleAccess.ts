/**
 * @deprecated Import from `@/hooks/usePermissions` or `@/constants/rolePermissions`.
 * Kept for backward compatibility.
 */
export {
  canAccessRoute,
  getNavItemsForRole as getRoutesForRole,
  isSchoolAdmin as isAdminRole,
  type AppRoute as NavRoute,
} from "@/constants/rolePermissions";
